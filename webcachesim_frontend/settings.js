const configDir = "../task_config_dir/";
const AUTH_FILE = configDir + "authentication.yaml";
const TRACE_FILE = configDir + "trace_params.yaml";
const CONFIG_FILE = configDir + "config_params.yaml";
const ALGORITHM_PARAM_FILE = configDir + "algorithm_params.yaml";

export { AUTH_FILE, TRACE_FILE, CONFIG_FILE, ALGORITHM_PARAM_FILE };
